{
  "name": "magic-mapper-webos",
  "private": true,
  "version": "0.2.0",
  "description": "A TV-native Homebrew wrapper for Magic Mapper",
  "scripts": {
    "check": "python3 -m unittest discover -s tests -v && python3 -m py_compile runtime/managed_mapper.py runtime/magic_mapper_runtime.py runtime/mapperctl.py vendor/magic_mapper.py",
    "package": "node scripts/package.mjs",
    "manifest": "node scripts/manifest.mjs",
    "sync-upstream": "node scripts/sync-upstream.mjs"
  },
  "devDependencies": {
    "@webos-tools/cli": "^3.2.1"
  }
}
